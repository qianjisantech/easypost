apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: easypost-app-ingress
spec:
  rules:
    - host: easypost.qianjisan.com  # 域名
      http:
        paths:
          - path: /app
            pathType: Prefix
            backend:
              service:
                name: easypost-app
                port:
                  number: 80